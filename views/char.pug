extends layout

block content
  h2 Character Sheet For #{char.alias()}
  table(class='big')
    tr
      th Attribute
      th Value
    each field in fields
      tr
        td #{field.display}
        td #{char[field.name]}
