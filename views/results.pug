extends layout

block content
  p
  h2 Current turn: #{char.display}
  h3 Attack result: #{attack.name}

    table
      tbody
        each result in results
          tr
            td #{result}

  form(method="get" action="encounter" class="inline")
    button(type="submit" name="submit_param" value="submit_value" class="link-button") Done